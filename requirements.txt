# Dependencias diretas com versoes fixas para builds reproduziveis.
# Para atualizar: alterar a versao aqui e testar; ou usar pip-tools.
Django==5.2.15
python-dotenv==1.2.2
cryptography==45.0.7
gunicorn==23.0.0
reportlab==4.5.1
Pillow==11.3.0
psycopg[binary]==3.3.4
# Monitorizacao opcional de erros/performance. So e ativado quando SENTRY_DSN
# esta definida no ambiente (ver config/monitoring.py).
sentry-sdk==2.62.0
